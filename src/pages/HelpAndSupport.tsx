import React from 'react';
import { Helmet } from 'react-helmet-async';

const HelpAndSupport: React.FC = () => (
  <div>
    <Helmet>
      <title>HelpAndSupport | StoneAge</title>
    </Helmet>
    <h1 className="text-2xl font-bold">HelpAndSupport</h1>
    <p>Here are your available HelpAndSupport.</p>
  </div>
);

export default HelpAndSupport;
